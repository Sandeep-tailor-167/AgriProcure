# Model artifacts

Step 11 writes versioned demand and waiting-time joblib bundles plus JSON metadata here. Artifacts and generated metadata are ignored by Git. Train only from documented sources and treat the included synthetic evaluation as pipeline evidence, not field performance.
