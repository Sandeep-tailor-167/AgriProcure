import {t}from'../js/i18n.js';import{api,post,patch}from'../js/api-client.js';import{loadingState,emptyState,errorState}from'../components/data-state.js';import{statusBadge}from'../components/status-badge.js';const safe=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));let user;
export function intelligencePage(currentUser){user=currentUser;return`<div class="page-shell"><div class="page-heading"><div><p class="eyebrow">${t('intelligence')}</p><h1>${t('aiIntelligence')}</h1><p>${t('aiIntelligenceHelp')}</p></div></div><form class="card filter-bar" data-demand-form><label class="field">${t('centres')}<select name="centreId" required></select></label><label class="field">${t('allCrops')}<select name="cropId" required></select></label><label class="field">${t('procurementDate')}<input type="date" name="targetDate" min="${new Date().toISOString().slice(0,10)}" required></label><label class="field">${t('capacity')}<input type="number" name="capacity" min="1" required></label><button class="button primary">${t('generateForecast')}</button></form><div data-intelligence-results>${loadingState()}</div><section><h2>${t('capacityRecommendations')}</h2><div data-recommendations>${loadingState()}</div></section></div>`;}
function render(items){const target=document.querySelector('[data-intelligence-results]');target.innerHTML=items.length?`<div class="booking-list">${items.map(x=>`<article class="card"><div class="centre-card-title"><h2>${safe(x.centre_name)}</h2>${statusBadge(safe(x.method))}</div><p>${safe(x.crop_name_en||'')} · ${safe(String(x.target_date||x.generated_at).slice(0,10))}</p><strong>${safe(x.prediction_value)} ${x.prediction_type==='demand'?t('predictedArrivals'):t('minutes')}</strong><p>${t('predictionRange')}: ${safe(x.lower_bound)}–${safe(x.upper_bound)}</p><small>${x.method==='model'?t('modelPrediction'):t('fallbackEstimate')} · ${safe(x.model_version||t('notAvailable'))}</small>${x.prediction_type==='demand'?`<button class="button secondary" data-recommend="${x.prediction_id}">${t('generateRecommendation')}</button>`:''}</article>`).join('')}</div>`:emptyState();target.querySelectorAll('[data-recommend]').forEach(b=>b.addEventListener('click',async()=>{await post('/recommendations/generate',{predictionId:b.dataset.recommend});await refreshRecommendations();}));}
async function refreshRecommendations(){const target=document.querySelector('[data-recommendations]'),items=await api('/recommendations');target.innerHTML=items.length?`<div class="booking-list">${items.map(x=>`<article class="card"><div class="centre-card-title"><h3>${safe(x.centre_name)} · ${safe(x.crop_name_en)}</h3>${statusBadge(safe(x.recommendation_status))}</div><p><strong>${safe(x.recommendation_type.replaceAll('_',' '))}</strong> · ${t('capacityGap')}: ${safe(x.capacity_gap)}</p><p>${safe(x.reason)}</p><small>${safe(x.predicted_demand)} ${t('predictedArrivals')} / ${safe(x.available_capacity)} ${t('capacity')}</small>${user.role==='admin'&&x.recommendation_status==='pending'?`<footer><button class="button primary" data-review="${x.recommendation_id}" data-decision="approved">${t('approve')}</button><button class="button ghost danger" data-review="${x.recommendation_id}" data-decision="rejected">${t('reject')}</button></footer>`:''}</article>`).join('')}</div>`:emptyState();target.querySelectorAll('[data-review]').forEach(b=>b.addEventListener('click',async()=>{const reason=prompt(t('reviewReason'));if(!reason)return;await patch(`/recommendations/${b.dataset.review}/review`,{decision:b.dataset.decision,reason});await refreshRecommendations();}));}
export async function loadIntelligence(){const form=document.querySelector('[data-demand-form]'),target=document.querySelector('[data-intelligence-results]');try{const[centres,crops,history]=await Promise.all([api('/centres'),api('/crops'),api('/predictions')]);form.centreId.innerHTML=centres.map(x=>`<option value="${x.centre_id}">${safe(x.centre_name)}</option>`).join('');form.cropId.innerHTML=crops.map(x=>`<option value="${x.crop_id}">${safe(x.crop_name_en)}</option>`).join('');render(history);await refreshRecommendations();form.addEventListener('submit',async e=>{e.preventDefault();const values=Object.fromEntries(new FormData(form));await post('/predictions/demand',values);render(await api('/predictions'));});}catch(e){target.innerHTML=errorState(e.message);}}
