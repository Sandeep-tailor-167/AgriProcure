'use strict';const {getPool}=require('../config/database');
async function demandFeatures(input,c=getPool()){const [rows]=await c.execute(`SELECT s.procurement_date,COUNT(a.appointment_id) bookings,SUM(q.check_in_time IS NOT NULL) arrivals,COALESCE(SUM(pt.accepted_quantity),0) quantity,COALESCE(SUM(q.queue_status='no_show')/NULLIF(SUM(q.check_in_time IS NOT NULL),0),0) no_show_rate FROM procurement_schedules s LEFT JOIN appointments a ON a.schedule_id=s.schedule_id AND a.booking_status<>'cancelled' LEFT JOIN queue_tokens q ON q.appointment_id=a.appointment_id LEFT JOIN procurement_transactions pt ON pt.appointment_id=a.appointment_id WHERE s.centre_id=? AND s.crop_id=? AND s.procurement_date<? GROUP BY s.procurement_date ORDER BY s.procurement_date DESC LIMIT 7`,[input.centreId,input.cropId,input.targetDate]);return rows;}
async function waitingFeatures(tokenId,actor,c=getPool()){const clauses=['q.token_id=?'];const values=[tokenId];if(actor.role==='farmer'){clauses.push('f.user_id=?');values.push(actor.userId);}else if(actor.role==='officer'){clauses.push(`EXISTS(SELECT 1 FROM officer_assignments oa WHERE oa.user_id=? AND oa.centre_id=s.centre_id AND oa.assignment_status='active' AND oa.ended_at IS NULL)`);values.push(actor.userId);}const [r]=await c.execute(`SELECT q.token_id,q.queue_date,s.centre_id,s.crop_id,q.priority_rank,q.check_in_time,COALESCE(ctrl.active_counters,1) active_counters,EXISTS(SELECT 1 FROM operational_interruptions oi WHERE oi.centre_id=s.centre_id AND oi.started_at<=UTC_TIMESTAMP() AND oi.ended_at IS NULL) interruption_active,(SELECT COUNT(*) FROM queue_tokens ahead JOIN appointments aa ON aa.appointment_id=ahead.appointment_id JOIN procurement_schedules ss ON ss.schedule_id=aa.schedule_id WHERE ss.centre_id=s.centre_id AND ahead.queue_date=q.queue_date AND ahead.queue_status='waiting' AND (ahead.priority_rank<q.priority_rank OR (ahead.priority_rank=q.priority_rank AND ahead.check_in_time<q.check_in_time))) farmers_ahead,COALESCE((SELECT AVG(TIMESTAMPDIFF(SECOND,recent.service_started_at,recent.service_completed_at))/60 FROM queue_tokens recent JOIN appointments ra ON ra.appointment_id=recent.appointment_id JOIN procurement_schedules rs ON rs.schedule_id=ra.schedule_id WHERE rs.centre_id=s.centre_id AND recent.service_completed_at>=UTC_TIMESTAMP()-INTERVAL 30 DAY),15) recent_avg_service_minutes FROM queue_tokens q JOIN appointments a ON a.appointment_id=q.appointment_id JOIN farmers f ON f.farmer_id=a.farmer_id JOIN procurement_schedules s ON s.schedule_id=a.schedule_id LEFT JOIN centre_queue_controls ctrl ON ctrl.centre_id=s.centre_id AND ctrl.queue_date=q.queue_date WHERE ${clauses.join(' AND ')}` ,values);return r[0]||null;}
async function modelVersion(type,version,metadata,c=getPool()){const [r]=await c.execute(`INSERT INTO ai_model_versions(model_type,version,training_source,metrics,model_status,trained_at) VALUES(?,?,?,CAST(? AS JSON),'active',UTC_TIMESTAMP(3)) ON DUPLICATE KEY UPDATE model_version_id=LAST_INSERT_ID(model_version_id),metrics=VALUES(metrics)`,[type,version,metadata.synthetic_model?'synthetic':'operational',JSON.stringify(metadata)]);return r.insertId;}
async function save(input,c=getPool()){const [r]=await c.execute(`INSERT INTO ai_predictions(model_version_id,prediction_type,centre_id,crop_id,token_id,target_date,prediction_value,lower_bound,upper_bound,method,input_features,generated_at) VALUES(?,?,?,?,?,?,?,?,?,?,CAST(? AS JSON),?)`,[input.modelVersionId||null,input.type,input.centreId,input.cropId||null,input.tokenId||null,input.targetDate||null,input.value,input.lowerBound,input.upperBound,input.method,JSON.stringify(input.features),new Date(input.generatedAt)]);return r.insertId;}
async function history(actor,c=getPool()){const clauses=[];const values=[];if(actor.role==='officer'){clauses.push(`EXISTS(SELECT 1 FROM officer_assignments oa WHERE oa.user_id=? AND oa.centre_id=p.centre_id AND oa.assignment_status='active' AND oa.ended_at IS NULL)`);values.push(actor.userId);}const [r]=await c.execute(`SELECT p.*,c.centre_name,crop.crop_name_en,m.version model_version FROM ai_predictions p JOIN procurement_centres c ON c.centre_id=p.centre_id LEFT JOIN crops crop ON crop.crop_id=p.crop_id LEFT JOIN ai_model_versions m ON m.model_version_id=p.model_version_id ${clauses.length?'WHERE '+clauses.join(' AND '):''} ORDER BY p.generated_at DESC LIMIT 100`,values);return r;}
module.exports={demandFeatures,waitingFeatures,modelVersion,save,history};
